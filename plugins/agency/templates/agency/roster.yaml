# Instance state. Belongs to this machine, not to the agency repo.
agency:
  root: {{AGENCY_ROOT_YAML}}
  opened: {{DATE}}

agents:
  - name: {{AGENT_NAME_YAML}}
    title: {{TITLE_YAML}}
    folder: {{AGENT_FOLDER_YAML}}
    session_id: {{SESSION_ID_YAML}}
    display_name: {{DISPLAY_NAME_YAML}}
    status: active
